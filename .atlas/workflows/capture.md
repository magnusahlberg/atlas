# capture

Append an item to `Inbox.md` without classification. Fastest possible capture.

---

Append the request to the end of `Inbox.md` as a new bullet line. Do not classify, do not file, do not propose. Just capture.

If the request is empty, ask me what to capture and append my answer.

After appending, confirm with the single line: `Atlas: Captured — <item>`. Nothing else.
